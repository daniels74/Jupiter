import { GlowifyDirective } from './glowify.directive';

describe('GlowifyDirective', () => {
  it('should create an instance', () => {
    const directive = new GlowifyDirective();
    expect(directive).toBeTruthy();
  });
});
